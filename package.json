{
  "name": "@opennetwork/dataset",
  "version": "5.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc --declaration && npm run tslint",
    "watch": "tsc -w",
    "tslint": "tslint -c tslint.json -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {},
  "dependencies": {
    "tsdef": "^0.0.13"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/opennetwork/dataset.git"
  },
  "devDependencies": {
    "esm": "^3.2.25",
    "tslint": "^5.19.0",
    "typescript": "^3.5.3"
  }
}
